{
  "name": "Handle",
  "version": "1.0.0",
  "main": "index.js",
  "author": "khirayama <https://github.com/khirayama>",
  "license": "MIT",
  "engines": {
    "node": ">=7.2"
  },
  "scripts": {
    "start": "npm run development",
    "clean": "rm -rf dist",
    "server": "NODE_PATH=dist node dist/server.js",
    "server:watch": "NODE_PATH=dist nodemon --config .nodemonrc.json dist/server.js",
    "compile:scripts": "babel src --out-dir dist",
    "compile:scripts:watch": "babel src --out-dir dist -w --source-maps inline",
    "copy:files": "cpx 'src/**/*.{json,woff2}' dist && mkdir dist/public",
    "copy:files:watch": "cpx -w 'src/**/*.json' dist",
    "bundle:scripts": "NODE_PATH=src browserify src/client.js -o dist/public/bundle.js -t babelify",
    "bundle:scripts:watch": "NODE_PATH=src watchify src/client.js -o dist/public/bundle.js -v -t babelify --debug",
    "build:scripts": "npm run compile:scripts && npm run copy:files && npm run bundle:scripts",
    "watch:scripts": "npm run compile:scripts && npm run copy:files && (npm run compile:scripts:watch & npm run copy:files:watch & npm run bundle:scripts:watch)",
    "build:styles": "postcss -c .postcssrc.js",
    "watch:styles": "postcss -c .postcssrc.js -w",
    "build": "npm run clean && npm run compile:scripts && npm run copy:files && (npm run bundle:scripts & npm run build:styles)",
    "watch": "npm run clean && npm run compile:scripts && npm run copy:files && (npm run compile:scripts:watch & npm run copy:files:watch & npm run bundle:scripts:watch & npm run watch:styles)",
    "application": "npm run clean && npm run compile:scripts && npm run copy:files && (npm run bundle:scripts & npm run build:styles & npm run server)",
    "development": "npm run clean && npm run compile:scripts && npm run copy:files && (npm run compile:scripts:watch & npm run copy:files:watch & npm run bundle:scripts:watch & npm run watch:styles & npm run server:watch)",
    "lint": "npm run lint:scripts && npm run lint:styles",
    "lint:fix": "npm run lint:scripts:fix && npm run lint:styles",
    "lint:scripts": "eslint --ignore-path .gitignore src",
    "lint:scripts:fix": "eslint --ignore-path .gitignore --fix src",
    "lint:styles": "stylelint --config .stylelintrc src/**/*.css"
  },
  "dependencies": {
    "@khirayama/circuit": "^1.0.0",
    "@khirayama/react-circuit": "^1.0.0",
    "autoprefixer": "^6.5.3",
    "axios": "^0.15.3",
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "babelify": "^7.3.0",
    "body-parser": "^1.15.2",
    "browser-sync": "^2.18.5",
    "browserify": "^13.1.1",
    "classnames": "^2.2.5",
    "cookie-parser": "^1.4.3",
    "cookie-session": "^2.0.0-alpha.2",
    "cpx": "^1.5.0",
    "cssnano": "^3.8.1",
    "eslint": "^3.11.1",
    "eslint-config-xo-react": "^0.10.0",
    "eslint-config-xo-space": "^0.15.0",
    "eslint-plugin-jsx-a11y": "^3.0.1",
    "eslint-plugin-react": "^6.8.0",
    "express": "^4.14.0",
    "express-useragent": "^1.0.4",
    "nodemon": "^1.11.0",
    "passport": "^0.3.2",
    "passport-instagram": "^1.0.0",
    "passport-twitter": "^1.0.4",
    "postcss-cli": "^2.6.0",
    "postcss-easy-import": "^1.0.1",
    "postcss-nested": "^1.0.0",
    "postcss-reporter": "^3.0.0",
    "postcss-simple-vars": "^3.0.0",
    "react": "^15.4.1",
    "react-addons-css-transition-group": "^15.4.1",
    "react-dom": "^15.4.1",
    "spectrometer": "^0.0.4",
    "stylelint": "^7.6.0",
    "stylelint-config-standard": "^15.0.0",
    "watchify": "^3.7.0"
  }
}
